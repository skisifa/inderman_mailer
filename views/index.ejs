<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <div class="container-fluid">
        <header class="terminal-header">
            <div class="header-content">
                <h1>Inderman Mailer v1.0</h1>
                <div class="terminal-controls">
                    <span class="control red"></span>
                    <span class="control yellow"></span>
                    <span class="control green"></span>
                </div>
            </div>
        </header>

        <div class="terminal-body">
            <div class="row">
                <div class="col-md-12">
                    <div class="terminal-status">
                        <div id="status-line" class="status-line">
                            [*] Ready to send emails | SMTPs: 0 | Emails: 0
                        </div>
                    </div>
                </div>
            </div>

            <div class="row mt-3">
                <div class="col-md-6">
                    <div class="terminal-section">
                        <div class="section-header">[ 1. Sender Configuration ]</div>
                        <div class="section-content">
                            <div class="form-group">
                                <label for="senderName">Sender Name:</label>
                                <input type="text" id="senderName" class="form-control terminal-input" placeholder="Display name (e.g., CyberGhost)">
                            </div>
                            <div class="form-group mt-2">
                                <label for="subject">Subject:</label>
                                <input type="text" id="subject" class="form-control terminal-input" placeholder="Subject line with tokens: {n:5} for digits, {nw:8} for alphanumeric">
                            </div>
                            <div class="form-group mt-2">
                                <label for="emailBody">Email Body:</label>
                                <div class="d-flex justify-content-between mb-1">
                                    <div>
                                        <button id="viewHtmlBtn" class="btn btn-outline-info btn-sm">View HTML</button>
                                        <button id="viewPreviewBtn" class="btn btn-outline-success btn-sm">View Preview</button>
                                        <button id="manageTemplatesBtn" class="btn btn-outline-primary btn-sm">Manage Templates</button>
                                    </div>
                                    <div>
                                        <label for="htmlFileUpload" class="btn btn-outline-warning btn-sm">Upload HTML</label>
                                        <input type="file" id="htmlFileUpload" accept=".html,.htm" style="display:none;">
                                    </div>
                                </div>
                                <textarea id="emailBodyHtml" class="form-control terminal-input" rows="10" style="font-family: 'Courier New', monospace; display:none;"></textarea>
                                <div id="emailBodyPreview" class="terminal-editor"></div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="terminal-section">
                        <div class="section-header">[ 2. SMTP Configuration ]</div>
                        <div class="section-content">
                            <div class="form-group">
                                <label for="smtpConfig">SMTP String (host|port|user|password):</label>
                                <div class="input-group">
                                    <input type="text" id="smtpConfig" class="form-control terminal-input" placeholder="smtp.example.com|587|user|password">
                                    <button id="addSmtp" class="btn btn-outline-success">Add</button>
                                </div>
                            </div>
                            <div class="form-group mt-3">
                                <label>Saved SMTPs:</label>
                                <select id="smtpSelect" class="form-control terminal-input" multiple>
                                    <% if (smtpConfigs && smtpConfigs.length > 0) { %>
                                        <% smtpConfigs.forEach(function(config) { %>
                                            <option value="<%= config.id %>"><%= config.host %>:<%= config.port %> | <%= config.user %></option>
                                        <% }); %>
                                    <% } else { %>
                                        <option disabled>No SMTPs configured</option>
                                    <% } %>
                                </select>
                                <div class="d-flex justify-content-end mt-2">
                                    <button id="deleteSmtp" class="btn btn-outline-danger btn-sm">Delete Selected</button>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row mt-3">
                <div class="col-md-12">
                    <div class="terminal-section">
                        <div class="section-header">[ 3. Email List Management ]</div>
                        <div class="section-content">
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="emailList">Paste Emails (one per line):</label>
                                        <textarea id="emailList" class="form-control terminal-input" rows="5" placeholder="user1@example.com&#10;user2@example.com"></textarea>
                                        <div class="d-flex justify-content-end mt-2">
                                            <button id="parseEmails" class="btn btn-outline-success btn-sm">Parse Emails</button>
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label for="emailFile">Upload Email List:</label>
                                        <input type="file" id="emailFile" class="form-control terminal-input" accept=".txt">
                                        <div class="d-flex justify-content-end mt-2">
                                            <button id="uploadEmails" class="btn btn-outline-success btn-sm">Upload</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="row mt-3">
                                <div class="col-md-12">
                                    <div class="table-responsive email-table-container">
                                        <table id="emailTable" class="table table-dark table-hover">
                                            <thead>
                                                <tr>
                                                    <th>#</th>
                                                    <th>Email</th>
                                                    <th>Actions</th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                <!-- Email entries will be added here dynamically -->
                                            </tbody>
                                        </table>
                                    </div>
                                    <div class="d-flex justify-content-between mt-2">
                                        <div>
                                            <span id="emailCount" class="badge bg-success">0 emails</span>
                                        </div>
                                        <div>
                                            <button id="addEmailBtn" class="btn btn-outline-success btn-sm">Add Email</button>
                                            <button id="clearEmails" class="btn btn-outline-danger btn-sm">Clear All</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row mt-3">
                <div class="col-md-12">
                    <div class="terminal-section">
                        <div class="section-header">[ 4. Actions ]</div>
                        <div class="section-content d-flex justify-content-between">
                            <button id="sendEmails" class="btn btn-success">SEND</button>
                            <button id="stopSending" class="btn btn-warning" disabled>STOP</button>
                            <button id="clearLogs" class="btn btn-danger">CLEAR LOGS</button>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row mt-3">
                <div class="col-md-12">
                    <div class="terminal-section">
                        <div class="section-header">[ 5. Live Terminal ]</div>
                        <div class="section-content">
                            <div id="terminal-logs" class="terminal-logs"></div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Modal for adding/editing email -->
    <div class="modal fade" id="emailModal" tabindex="-1" aria-labelledby="emailModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content terminal-modal">
                <div class="modal-header">
                    <h5 class="modal-title" id="emailModalLabel">Add Email</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="form-group">
                        <label for="modalEmail">Email Address:</label>
                        <input type="email" id="modalEmail" class="form-control terminal-input" placeholder="user@example.com">
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="button" id="saveEmail" class="btn btn-success">Save</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Template Management Modal -->
    <div class="modal fade" id="templateModal" tabindex="-1" aria-labelledby="templateModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-lg">
            <div class="modal-content bg-dark text-light">
                <div class="modal-header">
                    <h5 class="modal-title" id="templateModalLabel">Manage Email Templates</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="alert alert-info">
                        Use <code>{template}</code> in your email body to insert a random template from your collection.
                    </div>
                    <div class="mb-3">
                        <label for="templateName" class="form-label">Template Name:</label>
                        <input type="text" class="form-control bg-dark text-light" id="templateName" placeholder="Enter a name for this template">
                    </div>
                    <div class="mb-3">
                        <label for="templateContent" class="form-label">Template Content:</label>
                        <textarea class="form-control bg-dark text-light" id="templateContent" rows="5" placeholder="Enter template content (HTML allowed)"></textarea>
                    </div>
                    <button id="addTemplateBtn" class="btn btn-success">Add Template</button>
                    
                    <hr>
                    
                    <h6>Your Templates:</h6>
                    <div id="templatesList" class="mt-3">
                        <!-- Templates will be listed here -->
                        <div class="alert alert-secondary">No templates added yet.</div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>
